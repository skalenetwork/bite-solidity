{
    "name": "@skalenetwork/bite-solidity-example-encrypted-chat",
    "version": "1.0.0",
    "scripts": {
        "clean": "yarn run -T hardhat clean",
        "cleanCompile": "yarn clean && yarn compile",
        "compile": "yarn run -T hardhat compile",
        "hardhat": "yarn run -T hardhat"
    }
}
