# RPC endpoint for the SKALE chain
RPC_URL=http://localhost:8545

# BITE endpoint for threshold encryption operations
BITE_ENDPOINT=http://localhost:8545

# Deployed EncryptedMessenger contract address
CONTRACT_ADDRESS=0x...

# Demo user private keys (server-side only, never sent to browser)
USER1_PRIVATE_KEY=0x...
USER2_PRIVATE_KEY=0x...
